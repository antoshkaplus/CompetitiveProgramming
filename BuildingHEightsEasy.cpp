#include <algorithm>
#include <stack>
#include <queue>
#include <vector>
#include <iostream>
#include <string>

using namespace std;

struct BuildingHeightsEasy {
    int minimum(int M, vector<int> hs) {
        sort(hs.begin(), hs.end());
        
    }
};