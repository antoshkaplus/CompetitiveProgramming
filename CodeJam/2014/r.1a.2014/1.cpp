//
//  1.cpp
//  
//
//  Created by Anton Logunov on 8/4/14.
//
//

#include <stack>
#include <iostream>
#include <algorithm>
#include <string>
#include <map>
#include <vector>
#include <queue>

using namespace std;




int main(int argc, char* argv[]) {
    int T; 
    cin >> T;
    for (int t = 0; t < T; ++t) {
        int N, L;
        cin >> N >> L;
        
        string s;
        for (auto i = 0; i < N; ++i) {
            cin >> s;
            for (auto j = 0; j < L; ++j) {
                devives[] (s[j] == "0")
            }
        }
                   
        cout << "Case #" << t+1 << ": ";
        if (is_possible) {
            cout << move_count;
        } else {
            cout << "Fegla Won";
        }
        cout << endl;
    }
}













