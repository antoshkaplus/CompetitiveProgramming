


int main() {
    int T;
    cin >> T;
    for (auto test = 0; test < T; ++test) {
        int N;
        cin >> N;
        vector<int> hs(N);
        for (auto& h : hs) cin >> h;
        vector<int> ds(N-1);
        for (auto& d : ds) cin >> d;
        
        
        
    } 


}
